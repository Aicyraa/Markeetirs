<div>
   <span> <%= item.item %> </span>
   <span> <%= item.price %> </span>
   <span> <%= item.stock %> out of <%= item.total_stock %> </span>
   <span> <%= item.cost %> </span>
   <span> <%= item.unit %> </span>
   <span> <%= item.status %> </span>
   <span> <%= item.category %> </span>
</div>
